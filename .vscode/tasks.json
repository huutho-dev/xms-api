{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build runner",
			"type": "shell",
			"command": "pub run build_runner build --delete-conflicting-outputs",
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}